#[derive(PartialEq, Debug)]
pub enum Token {
    Assign,
    Asterisk,
    Bang,
    Comma,
    Else,
    Equal,
    False,
    Function,
    GT,
    Identifier(Vec<u8>),
    If,
    Illegal(Vec<u8>),
    Integer(Vec<u8>),
    LeftBrace,
    Let,
    LeftParen,
    LT,
    Minus,
    NotEqual,
    Plus,
    Return,
    RightBrace,
    RightParen,
    Semicolon,
    Slash,
    True,
}
