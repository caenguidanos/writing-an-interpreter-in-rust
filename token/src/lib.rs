#[derive(Clone, Debug, PartialEq)]
pub enum Token {
    Assign,
    Asterisk,
    Bang,
    Comma,
    Else,
    Equal,
    False,
    Function,
    GreaterThan,
    GreaterThanOrEqual,
    Identifier(Vec<u8>),
    If,
    Illegal(Vec<u8>),
    Integer(Vec<u8>),
    LeftBrace,
    Let,
    LeftParen,
    LessThan,
    LessThanOrEqual,
    Minus,
    NotEqual,
    Plus,
    Pow,
    Return,
    RightBrace,
    RightParen,
    Semicolon,
    Slash,
    True,
}
